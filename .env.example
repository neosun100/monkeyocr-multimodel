# MonkeyOCR Configuration
# Copy this file to .env and modify as needed

# Service port (check availability before setting)
PORT=7870

# GPU Configuration
# Auto-selected by start.sh, or set manually
NVIDIA_VISIBLE_DEVICES=0

# GPU idle timeout in seconds (release VRAM after idle)
GPU_IDLE_TIMEOUT=600

# Data directory for uploads/outputs
DATA_DIR=/tmp/monkeyocr

# Docker image name
DOCKER_IMAGE=neosun/monkeyocr:latest

# Model download source: huggingface or modelscope
MODEL_SOURCE=modelscope

# Model name: MonkeyOCR-pro-3B, MonkeyOCR-pro-1.2B, or MonkeyOCR
MODEL_NAME=MonkeyOCR-pro-3B
